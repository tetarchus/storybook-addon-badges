export { defaultBadgeConfig, defaultBadgesConfig } from '@/config';
export {
  ADDON_ID,
  ADDON_TITLE,
  BADGE,
  BADGE_LOCATION,
  PARAM_BADGES_KEY,
  PARAM_CONFIG_KEY,
  TOOL_ID,
  TOOL_EXTRA_ID,
} from '@/constants';

export type {
  BadgeConfig,
  BadgeLocation,
  BadgesMap,
  NewBadgesConfig as BadgesConfig,
  TooltipConfig,
  TooltipMessageProps,
} from '@/types';
