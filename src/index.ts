export { defaultBadgesConfig, defaultBadgeConfig } from '@/config';
export {
  ADDON_ID,
  ADDON_TITLE,
  BADGE,
  PARAM_BADGES_KEY,
  PARAM_CONFIG_KEY,
  TOOL_ID,
} from './constants';

export type {
  BadgeConfig,
  BadgesMap as BadgesConfig,
  TooltipConfig,
  TooltipMessageProps,
} from '@/types';
