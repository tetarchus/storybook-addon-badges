export { defaultBadgesConfig, defaultBadgeConfig } from '@/config';
export {
  ADDON_ID,
  ADDON_TITLE,
  BADGE,
  BADGE_LOCATION,
  PARAM_BADGES_KEY,
  PARAM_CONFIG_KEY,
  TOOL_ID,
  TOOL_EXTRA_ID,
} from './constants';

export type {
  BadgeConfig,
  BadgeLocation,
  BadgesMap as BadgesConfig,
  NewBadgesConfig,
  TooltipConfig,
  TooltipMessageProps,
} from '@/types';
