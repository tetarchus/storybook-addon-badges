export { defaultBadgeConfig, defaultConfig as defaultBadgesConfig } from '@/config';
export {
  ADDON_ID,
  ADDON_TITLE,
  BADGE,
  BADGE_LOCATION,
  PARAM_BADGES_KEY,
  PARAM_CONFIG_KEY,
  TOOL_ID,
  TOOL_EXTRA_ID,
} from '@/constants';

export type {
  Badge as BadgeConfig,
  BadgeLocation,
  BadgeMap as BadgesMap,
  BadgesConfig,
  TooltipConfig,
  TooltipMessageProps,
} from '@/types';
