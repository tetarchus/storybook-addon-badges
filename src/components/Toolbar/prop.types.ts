/** Props for the Toolbar component. */
type ToolbarProps = {
  /** ID to use for tests. */
  'data-testid'?: string | undefined;
  /** Whether this is rendering in the `toolbar-extra` location. */
  end?: boolean;
};

export type { ToolbarProps };
