import { styled } from '@storybook/theming';

/** Wrapper for the label to maintain styles with badges. */
const LabelWrapper = styled.div`
  align-items: flex-start;
  display: flex;
  flex-direction: column;
  justify-content: center;
`;

export { LabelWrapper };
